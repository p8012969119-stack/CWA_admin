[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["echo 'No dependencies required'"]

[phases.build]
cmds = ["npm run check"]

[start]
cmd = "npm start"

[variables]
NODE_ENV = "production"
ADMIN_API_BASE_URL = "https://api.crackwithai.net/api"
